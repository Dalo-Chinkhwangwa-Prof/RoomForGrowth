<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:cardCornerRadius="8dp"
    android:layout_margin="2dp"
    app:cardElevation="4dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">
<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content">


    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/topic_edittext"
        android:singleLine="true"
        android:imeOptions="actionNext"
        android:layout_margin="16dp"
        app:layout_constraintTop_toTopOf="parent"
        android:hint="Topic Name"
        />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/difficulty_edittext"
        android:singleLine="true"
        android:imeOptions="actionDone"
        android:layout_margin="16dp"
        app:layout_constraintTop_toBottomOf="@id/topic_edittext"
        android:hint="Topic Difficulty"
        android:inputType="number"
        />

    <Button
        android:id="@+id/insert_button"
        android:layout_margin="16dp"
        android:padding="8dp"
        android:text="Insert"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/difficulty_edittext"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>